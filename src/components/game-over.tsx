/**
 * 🔧 TASK 3: Create the Game Over screen.
 *
 * This component should:
 *   - Display "Game Over" with the player's final score
 *   - Show the high score
 *   - Provide a "Play Again" button that calls onRestart
 *   - Use the same overlay style as StartScreen (see styles.overlay)
 *
 * Props you'll need:
 *   - score: number
 *   - highScore: number
 *   - onRestart: () => void
 *
 * Hint: look at StartScreen.tsx for reference on structure and styling.
 */

export default function GameOver() {
  return <div>{/* Implement me! */}</div>;
}
