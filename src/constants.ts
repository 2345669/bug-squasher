
export const GAME_WIDTH = 600;
export const GAME_HEIGHT = 500;

export const BUG_EMOJIS = ["🐛", "🪲", "🐜", "🦗", "🪳", "🕷️"];

export const CODE_SNIPPETS = [
  'const bug = "features";',
  "if (friday) deploy();",
  "// TODO: fix later",
  "catch (e) { /* ¯\\_(ツ)_/¯ */ }",
  'git push --force // YOLO',
  "while(true) { hope(); }",
  'const isWorking = "maybe";',
  "// works on my machine",
  "rm -rf node_modules && pray",
  'console.log("here 47");',
  "setTimeout(fix, Infinity);",
  "// I have no idea why this works",
];

export const INITIAL_LIVES = 5;
export const SPAWN_INTERVAL_MS = 1800;
export const TICK_INTERVAL_MS = 50;